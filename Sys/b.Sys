#! /usr/bin/env bash
set -uvx
set -e
cd "$(dirname "$0")"
cwd=`pwd`
cd $cwd
rm -rf *.nupkg
nuget-gen Sys.cs
cd $cwd/build-Sys
dotnet pack -o .. -p:Configuration=Release -p:Platform="Any CPU" Sys.csproj
cd $cwd
ls -ltr *.nupkg
